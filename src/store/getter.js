const getter = {
  // getCount(state){
  //   return state.count+2
  // }
//   userEid: (state) => state.eid,
//   userEnterpriseName: (state) => state.enterpriseName,
//   userLoginAccount: (state) => state.loginAccount,

//   // getters 只会依赖 state 中的成员去更新
//   companyList: (state) => {
//     return state.app.orgList.filter(item => item.t == 1)
//   },
}
export default getter